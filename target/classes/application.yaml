spring:
  application:
    name: gestion-transacciones
  spring:
    datasource:
      url: jdbc:mysql://mysql:3306/mydb
      username: root
      password: root
    jpa:
      hibernate:
        ddl-auto: update
      show-sql: true